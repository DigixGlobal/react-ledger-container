export const DEFAULT_KD_PATH = "44'/60'/0'/";
export const TIMEOUT_DEFAULT = 5 * 1000; // timeout for when user disconnects
export const TIMEOUT_SIGNING = 60 * 1000; // timeout for when user is shown signing message
export const POLL_CONNECTED = 10 * 1000; // poll speed when device is ready
export const POLL_DISCONNECTED = 1 * 1000; // poll speed when device isn't ready
